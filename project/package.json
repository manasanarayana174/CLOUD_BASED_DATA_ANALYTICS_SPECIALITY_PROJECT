{
    "name": "hospital-ai-dashboard",
    "version": "1.0.0",
    "description": "AI-Powered Hospital Dashboard",
    "scripts": {
        "start": "echo 'Please install dependencies in client and server first!'",
        "install-all": "npm install --prefix client && npm install --prefix server",
        "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\""
    },
    "devDependencies": {
        "concurrently": "^8.2.0"
    }
}